# Dotfiles role

This role clones a pinned commit of `matheuseabra/dotfiles` as `dev` and stows only portable packages. It intentionally does not deploy the macOS `zsh`, Ghostty, Herdr, OpenCode, Karabiner, skhd, Cava, Druk, wallpaper, or Keychain configuration.
